{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "pip install fastapi uvicorn[standard] langchain langchain-groq langchain-community langgraph tavily-python pydantic python-dotenv python-multipart httpx && uvicorn api.main:app --host 0.0.0.0 --port $PORT --workers 2"
  }
}